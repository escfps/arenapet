workflows:
  ios-appstore:
    name: iOS App Store Release
    max_build_duration: 90
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: Codemagic
    environment:
      groups:
        - ios_signing
      vars:
        APP_STORE_APPLE_ID: 6772081915
        BUNDLE_ID: "com.arenapet.app"
        XCODE_WORKSPACE: "ios/App/App.xcworkspace"
        XCODE_SCHEME: "App"
      node: 22.12.0
      xcode: latest
      cocoapods: default
    scripts:
      - name: Install dependencies
        script: npm install
      - name: Install Capacitor deps
        script: npm install @capacitor/core @capacitor/ios @capacitor/cli
      - name: Build web assets
        script: |
          npm run build
          echo "=== Build output ==="
          ls -la
          echo "=== Checking common output dirs ==="
          ls -la dist 2>/dev/null || echo "no dist"
          ls -la .output 2>/dev/null || echo "no .output"
          ls -la build 2>/dev/null || echo "no build"
      - name: Init Capacitor
        script: ./node_modules/.bin/cap init "Arena Pet" com.arenapet.app --web-dir dist/client
      - name: Add and sync iOS
        script: |
          ./node_modules/.bin/cap add ios
          ./node_modules/.bin/cap copy ios
          ./node_modules/.bin/cap sync ios
      - name: Install pods
        script: |
          cd ios/App
          pod install
      - name: Set up code signing
        script: |
          keychain initialize
          echo $CM_CERTIFICATE | base64 --decode > /tmp/certificate.p12
          keychain add-certificates --certificate /tmp/certificate.p12 --certificate-password $CM_CERTIFICATE_PASSWORD
          echo $CM_PROVISIONING_PROFILE | base64 --decode > /tmp/profile.mobileprovision
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp /tmp/profile.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/
    artifacts:
      - ios/App/build/ios/ipa/*.ipa
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        submit_to_app_store: true
        release_type: AFTER_APPROVAL
